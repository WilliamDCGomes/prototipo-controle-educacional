enum Flavor {
  DEV,
  HMLG,
  PROD,
}

enum quickActionsOptions {
  gradesFaults,
  studentCard,
  onlineStudentCard,
  academicCalendar,
  academicRecord,
  schoolStatement,
  newsEvents,
}

enum academicTabs {
  curriculum,
  deliveries
}

enum creditDebtCardType {
  mastercard,
  visa
}

enum studentTypeRequest {
  studentCard,
  schoolStatement
}

enum paymentMethod {
  creditCard,
  bankSlip
}